/**
 * firestore collection/sub-collection Ids
 */
const fireIds = {
  vendors: "vendors",
  users: "users",
  orders: "orders",
  kpTransactions: "kp_transactions",
  wallets: "wallets",
}

module.exports = { fireIds }
